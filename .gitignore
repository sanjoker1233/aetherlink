node_modules/
.next/
.next_stale_*/
*.local
.env
.env.local
backend/cryptmessenger-server
frontend/out/
/tmp/cryptmessenger-db.json
*.log
.DS_Store

# translation safety backup
frontend_pre_english_backup/
backend/aetherlink-server

# go build default output (regenerate with: go build ./cmd/server/)
backend/server

# runtime secrets (JWT secret generated at startup) — NEVER commit
.jwt.env

# runtime server data (SQLite with user keys/contacts) — NEVER commit
backend/data/

# TypeScript incremental build cache
*.tsbuildinfo
